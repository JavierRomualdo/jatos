import { NgDropFileDirective } from './ng-drop-file.directive';

describe('NgDropFileDirective', () => {
  it('should create an instance', () => {
    const directive = new NgDropFileDirective();
    expect(directive).toBeTruthy();
  });
});
